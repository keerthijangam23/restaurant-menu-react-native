import { StyleSheet } from "react-native";
export const TimingModelStyles = StyleSheet.create({
    container: { backgroundColor: "#000000aa", flex: 1 },
    innerContainer: { backgroundColor: "white", margin: 100 },
  });